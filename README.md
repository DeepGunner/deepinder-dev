# deepinder.dev

Personal landing page. React + Vite + TypeScript.

```bash
npm install
npm run dev
```

Build:
```bash
npm run build
npm run preview
```

Deploys automatically on push to main via Cloudflare Pages.
